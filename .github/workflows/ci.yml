name: CI - Testes da Calculadora ✅ # <-- Esse é o nome que aparecerá no GitHub como "CI - Testes da Calculadora"

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  testes-unitarios:
    name: Testes com Jest 🧪 # <-- Esse é o nome que aparecerá como "Testes com Jest 🧪"
    runs-on: ubuntu-latest

    steps:
      - name: Clonar repositório
        uses: actions/checkout@v3

      - name: Instalar Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Instalar dependências
        run: npm ci

      - name: Rodar Lint
        run: npm run lint

      - name: Rodar Testes
        run: npm run test

      - name: Verificar build
        run: npm run build
